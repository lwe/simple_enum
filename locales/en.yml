# english translation
en:
  activerecord:
    errors:
      messages:
        invalid_enum: invalid option supplied
  mongoid:
    errors:
      messages:
        invalid_enum: invalid option supplied
